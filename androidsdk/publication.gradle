
publishing {
    publications {
        release(MavenPublication) {
            groupId = project.sdkGroupId
            artifactId = project.sdkArtifactId
            version = project.sdkVersion

            afterEvaluate {
                from components.release
            }
        }

        //add repo configuration here
    }
}
